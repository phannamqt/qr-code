version: '3.9'

services:
  app:
    image: phannamit/qr-code:latest
    build: .
    container_name: qr-code-app
    ports:
      - '3020:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
